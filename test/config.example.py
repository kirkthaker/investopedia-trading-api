"""Demo config for feature tests.

Since the feature tests actually call the API, we need a way to authenticate
to run them

Usage:
    - Copy config.example.py to config.py (will be in .gitignore)
    - Update the values with your values (create a new account to test or use your own)
"""
test_username = "yourtestusername"
test_password = "yourtestpassword"

